#define F_CPU 20000000UL
#ifndef __AVR_ATmega2560__
#define __AVR_ATmega2560__
#endif
#include <stdio.h>
#include <stdlib.h> // the header of the general-purpose standard library of C programming language
#include <avr/io.h> // the header of the I/O port library

#include <util/delay_basic.h> // header of the delay functions
#include <util/delay.h>
#include "lcd_test.h"

// #include <avr/interrupt.h> // header for interrupts

int main(int argc, char *argv[])
{ 
    DDRC = 0b11111111; // Sets all pins on Port L to output 
    PORTC = 0b00000000; // initialize pins to high to turn on LEDs (1 Yel & 1 Grn) 
    _delay_ms(1000);
    PORTL = 0b10000000; // initialize pins to high to turn on LEDs (1 Yeldada & 1 Grn) 
    _delay_ms(1000);
    PORTL = 0b10100000; // initialize pins to high to turn on LEDs (1 Yel & 1 Grn) 
    _delay_ms(1000);
    PORTL = 0b10000000; // initialize pins to high to turn on LEDs (1 Yel & 1 Grn) 
    return (0); // This line returns a 0 value to the calling program 
    //sei();
    test_LCD();
// generally means no error was returned 
}
